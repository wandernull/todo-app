{
  "name": "claude-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute todos-db --local --file=migrations/0001_create_todos.sql",
    "db:migrate:prod": "wrangler d1 execute todos-db --file=migrations/0001_create_todos.sql"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241224.0",
    "typescript": "^5.5.2",
    "wrangler": "^3.95.0"
  }
}
